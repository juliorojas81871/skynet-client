create a folder and make sure that you put it with the server
Get all the data for the .env.local file.
On a terminal by itself type 'npm run dev'

Note to self, you need to add the bot to the chat manually 